# 接口对接状态清单

## 总体进度

- **已对接**：20/21 (95%)
- **未对接**：1/21 (5%)
- **算法接口**：3/3 (100%) - 已由算法服务提供

---

## ✅ 已对接接口（20个）

### 1. 认证与角色模块（3/3）

| 接口 | 方法 | 路径 | 状态 | 服务文件 |
|------|------|------|------|----------|
| 用户注册 | POST | `/api/auth/register` | ✅ | `authService.ts` |
| 用户登录 | POST | `/api/auth/login` | ✅ | `authService.ts` |
| 获取用户信息 | GET | `/api/auth/profile` | ✅ | `authService.ts` |

**说明**：认证模块已完全对接，包括注册、登录和获取用户信息功能。

---

### 2. 患者管理模块（2/2 已对接 ✅）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 获取患者列表 | GET | `/api/patients` | ✅ | 高 | 护工/子女角色需要 |
| 获取患者详情 | GET | `/api/patients/{id}` | ✅ | 中 | 查看患者详细信息（已实现查看详情功能） |

**当前状态**：
- ✅ `GET /api/patients` - 已在护工/子女首页使用，从后端加载患者列表
- ✅ `GET /api/patients/{id}` - 已实现查看详情功能，点击"查看详情"按钮在 Modal 中展示患者完整信息

**已创建**：`src/services/patientService.ts` - 包含 `getPatients()` 和 `getPatientDetail(id)` 函数

---

### 3. 用药计划模块（4/4 已对接 ✅）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 查询计划列表 | GET | `/api/schedules?patientId={id}` | ✅ | 高 | 核心功能 |
| 创建计划 | POST | `/api/schedules` | ✅ | 高 | 核心功能 |
| 更新计划 | PATCH | `/api/schedules/{id}` | ✅ | 高 | 核心功能 |
| 启停计划 | POST | `/api/schedules/{id}/toggle` | ✅ | 高 | 核心功能 |

**当前状态**：
- ✅ `planService.ts` - 已实现4个接口函数，包含数据映射
- ✅ `PlanBoardView.vue` - 已更新使用真实接口
- ✅ 启停计划功能已实现并测试通过
- ⏳ 新增计划和编辑计划功能待实现（接口已对接，UI待完善）

**已创建**：`src/services/planService.ts` - 包含完整的接口对接和数据映射

---

## ❌ 未对接接口（1个）

---

### 4. 事件与历史模块（3/4 已对接 ✅，1/4 待对接）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 查询事件列表 | GET | `/api/intake-events?patientId={id}&range={range}` | ✅ | 高 | 核心功能 |
| 创建事件 | POST | `/api/intake-events` | ⏳ | 高 | 待接入检测页 |
| 确认事件 | POST | `/api/intake-events/{id}/confirm` | ✅ | 高 | 核心功能 |
| 统计报表 | GET | `/api/reports/summary?patientId={id}&range={range}` | ✅ | 中 | 统计功能 |

**当前状态**：
- ✅ `historyService.ts` 已改为真实接口：包含事件列表、统计报表、手动确认，同时保留 `createIntakeEvent` 供检测页调用
- ✅ `HistoryCenterView.vue` 已对接真实数据，并在时间线中支持“手动确认”按钮
- ⏳ `DetectionRoomView.vue` 仍使用 TODO，需要接入 `createIntakeEvent`

**需要继续修改**：`src/views/DetectionRoomView.vue`（事件上报流程）

---

### 5. 异常与告警模块（2/2 已对接 ✅）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 查询告警列表 | GET | `/api/alerts?patientId={id}` | ✅ | 中 | 告警功能 |
| 标记已处理 | POST | `/api/alerts/{id}/resolve` | ✅ | 中 | 告警处理 |

**当前状态**：
- ✅ `alertService.ts` - 已实现2个接口函数，包含数据映射
- ✅ `AlertCenterView.vue` - 已更新使用真实接口
- ✅ 支持患者切换自动刷新告警列表
- ✅ 标记已处理功能已实现并测试通过

**已创建**：`src/services/alertService.ts` - 包含完整的接口对接和数据映射

---

### 6. 日志图片模块（2/2 已对接 ✅）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 上传图片 | POST | `/api/logs/images` | ✅ | 低 | multipart/form-data |
| 查询图片列表 | GET | `/api/logs/images?eventId={id}` | ✅ | 低 | 历史详情查看 |

**当前状态**：
- ✅ `logService.ts` - 已实现2个接口函数，包含文件验证和URL解析
- ✅ 支持文件类型验证（JPG、PNG、WEBP）
- ✅ 支持文件大小验证（≤2MB）
- ⏳ `DetectionRoomView.vue` 待接入上传图片功能
- ✅ `HistoryCenterView.vue` 已支持显示图片（通过 historyService）

**已创建**：`src/services/logService.ts` - 包含完整的接口对接和工具函数

---

### 7. 设置管理模块（2/2 已对接 ✅）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 获取设置 | GET | `/api/settings` | ✅ | 中 | 设置功能 |
| 更新设置 | PUT | `/api/settings` | ✅ | 中 | 设置功能 |

**当前状态**：
- ✅ `settingsService.ts` - 已实现2个接口函数，包含数据映射
- ✅ `SettingsCenterView.vue` - 已更新使用真实接口
- ✅ 支持 fallbackMode 格式转换（后端大写 ↔ 前端小写）
- ✅ 更新设置后自动同步服务器返回的最新值

**已创建**：`src/services/settingsService.ts` - 包含完整的接口对接和数据映射

---

### 8. 状态检查模块（2/2 已对接 ✅）

| 接口 | 方法 | 路径 | 状态 | 优先级 | 说明 |
|------|------|------|------|--------|------|
| 联通性测试 | GET | `/api/status/ping` | ✅ | 低 | 健康检查 |
| 角色权限矩阵 | GET | `/api/status/role` | ✅ | 低 | 权限缓存 |

**当前状态**：
- ✅ `statusService.ts` - 已实现2个接口函数
- ✅ 可选功能，不影响核心业务
- ⏳ 可在需要时调用（如：应用启动时检查服务状态）

**已创建**：`src/services/statusService.ts` - 包含健康检查和权限查询功能

---

## 🔧 算法服务接口（已对接，3/3）

| 接口 | 方法 | 路径 | 状态 | 服务文件 |
|------|------|------|------|----------|
| 健康检查 | GET | `/health` | ✅ | `detectionService.ts` |
| 就绪检查 | GET | `/ready` | ✅ | `detectionService.ts` |
| 检测预测 | POST | `/v1/detections/predict` | ✅ | `detectionService.ts` |

**说明**：算法服务接口已由算法端提供，前端已对接完成。

---

## 📋 对接优先级建议

### 🔴 高优先级（核心功能，10个接口）

1. **用药计划模块**（4个接口）
   - `GET /api/schedules` - 查询计划列表
   - `POST /api/schedules` - 创建计划
   - `PATCH /api/schedules/{id}` - 更新计划
   - `POST /api/schedules/{id}/toggle` - 启停计划

2. **事件与历史模块**（4个接口）
   - `GET /api/intake-events` - 查询事件列表
   - `POST /api/intake-events` - 创建事件
   - `POST /api/intake-events/{id}/confirm` - 确认事件
   - `GET /api/reports/summary` - 统计报表

3. **患者管理模块**（2个接口）
   - `GET /api/patients` - 获取患者列表
   - `GET /api/patients/{id}` - 获取患者详情

### 🟡 中优先级（重要功能，0个接口）

✅ 已完成所有中优先级接口对接

### 🟢 低优先级（辅助功能，0个接口）

✅ 已完成所有低优先级接口对接

---

## 📝 对接步骤建议

### 第一步：患者管理模块
1. 创建 `src/services/patientService.ts`
2. 实现 `getPatients()` 和 `getPatientDetail(id)`
3. 更新 `usePatientList.ts` composable
4. 更新 `CaregiverDashboardView.vue` 和 `ChildDashboardView.vue`

### 第二步：用药计划模块
1. 修改 `src/services/planService.ts`
2. 替换所有 Mock 数据为真实接口调用
3. 更新 `PlanBoardView.vue` 使用真实数据
4. 添加创建计划功能（目前只有占位按钮）

### 第三步：事件与历史模块
1. 修改 `src/services/historyService.ts`
2. 替换所有 Mock 数据为真实接口调用
3. 更新 `HistoryCenterView.vue` 使用真实数据
4. 在 `DetectionRoomView.vue` 中实现事件上报

### 第四步：异常与告警模块 ✅
1. ✅ 修改 `src/services/alertService.ts`
2. ✅ 替换所有 Mock 数据为真实接口调用
3. ✅ 更新 `AlertCenterView.vue` 使用真实数据

### 第五步：设置管理模块 ✅
1. ✅ 修改 `src/services/settingsService.ts`
2. ✅ 替换所有 Mock 数据为真实接口调用
3. ✅ 更新 `SettingsCenterView.vue` 使用真实数据

### 第六步：日志图片模块 ✅
1. ✅ 创建 `src/services/logService.ts`
2. ✅ 实现图片上传和查询功能
3. ⏳ 在检测页面中接入上传功能（待实现）
4. ✅ 历史页面已支持显示图片

---

## 🔍 检查清单

### 需要创建的服务文件
- [x] `src/services/patientService.ts` ✅
- [x] `src/services/logService.ts` ✅
- [x] `src/services/statusService.ts` ✅

### 需要修改的服务文件
- [x] `src/services/planService.ts` - 替换所有 Mock ✅
- [x] `src/services/historyService.ts` - 替换所有 Mock ✅
- [x] `src/services/alertService.ts` - 替换所有 Mock ✅
- [x] `src/services/settingsService.ts` - 替换所有 Mock ✅

### 需要更新的视图文件
- [x] `src/views/PlanBoardView.vue` - 对接计划接口 ✅
- [x] `src/views/HistoryCenterView.vue` - 对接历史接口 ✅
- [x] `src/views/AlertCenterView.vue` - 对接告警接口 ✅
- [x] `src/views/SettingsCenterView.vue` - 对接设置接口 ✅
- [x] `src/views/CaregiverDashboardView.vue` - 对接患者接口 ✅
- [x] `src/views/ChildDashboardView.vue` - 对接患者接口 ✅
- [ ] `src/views/DetectionRoomView.vue` - 对接事件上报接口

### 需要更新的 Composable
- [ ] `src/composables/usePatientList.ts` - 对接患者接口

---

## 📊 统计信息

### 按模块统计

| 模块 | 总接口数 | 已对接 | 未对接 | 完成率 |
|------|----------|--------|--------|--------|
| 认证与角色 | 3 | 3 | 0 | 100% |
| 患者管理 | 2 | 2 | 0 | 100% |
| 用药计划 | 4 | 4 | 0 | 100% |
| 事件与历史 | 4 | 3 | 1 | 75% |
| 异常与告警 | 2 | 2 | 0 | 100% |
| 日志图片 | 2 | 2 | 0 | 100% |
| 设置管理 | 2 | 2 | 0 | 100% |
| 状态检查 | 2 | 2 | 0 | 100% |
| **总计** | **21** | **20** | **1** | **95%** |

### 按优先级统计

| 优先级 | 接口数 | 占比 |
|--------|--------|------|
| 高优先级 | 10 | 48% |
| 中优先级 | 4 | 19% |
| 低优先级 | 4 | 19% |
| 算法接口 | 3 | 14% |

---

## 🎯 总结

**当前状态**：
- ✅ 认证模块已完全对接，可以正常登录、注册、获取用户信息
- ❌ 其他业务模块全部使用 Mock 数据，需要逐步对接

**建议**：
1. 优先对接高优先级接口（10个），确保核心功能可用
2. 然后对接中优先级接口（4个），完善功能体验
3. 最后对接低优先级接口（4个），提升完整度

**预计工作量**：
- 高优先级：约 2-3 天
- 中优先级：约 1-2 天
- 低优先级：约 1 天
- **总计**：约 4-6 天

